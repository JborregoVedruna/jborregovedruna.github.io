name: 01-Firstworkflow
on:
    push:
      branches:
        - 'master'
    workflow_dispatch:
jobs:
    joaquin:
        runs-on: ubuntu-latest
        steps:
            - run: echo 'Hello world'
            
